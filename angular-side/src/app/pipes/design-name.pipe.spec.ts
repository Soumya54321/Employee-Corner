import { DesignNamePipe } from './design-name.pipe';

describe('DesignNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DesignNamePipe();
    expect(pipe).toBeTruthy();
  });
});
