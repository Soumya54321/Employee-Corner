import { ModificationPipe } from './modification.pipe';

describe('ModificationPipe', () => {
  it('create an instance', () => {
    const pipe = new ModificationPipe();
    expect(pipe).toBeTruthy();
  });
});
